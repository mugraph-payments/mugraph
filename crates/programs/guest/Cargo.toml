[package]
name = "mugraph-programs-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
mugraph-core = { path = "../../core", default-features = false }

risc0-zkvm = { version = "1.0.3" }
sha2 = { version = "0.10.8", default-features = false }
