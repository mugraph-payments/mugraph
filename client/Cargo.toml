[package]
name = "mugraph-client"
version = "0.0.1"
edition = "2021"

[dependencies]
mugraph-core = { path = "../core" }

serde = { version = "1.0.204", features = ["derive"] }
tracing = "0.1.40"
tracing-subscriber = "0.3.18"
