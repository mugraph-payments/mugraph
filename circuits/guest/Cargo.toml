[package]
name = "mugraph-circuits-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
mugraph-core = { path = "../../core" }
mugraph-crypto = { path = "../../crypto" }
risc0-zkvm = { workspace = true }
